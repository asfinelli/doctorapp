

<% if current_doctor %>
<%= render partial: 'show' %>
<%= form_for @appointment do |f| %>

<div class="field">
  <%= f.label :doctor_id %><br />
  <%= f.hidden_field :doctor_id, required: true, value: current_doctor.id %>
</div>
<p>
  <div class="field">
  <div class="follow-up">
    <%= f.label :follow_up %>
  <div class="input">

    <%= f.text_field :follow_up, required: true %>
  <p>
    <%= f.submit 'Confirm' %>
  </p>
  <% end %>
  <% end %>
</div>
</div>
</div>
</div>
